<table>
  <tbody>
    <% @post_comments.each do |comment| %>
 <div class="col-3 text-center">
   <%#= attachment_image_tag comment.user, :profile_image, fallback: "no_image.jpeg", size: '40x40' , class:"rounded-circle"; %>

    <div class="small mb-4"><%= comment.user.user_name %>
   </div>
　</div>
　<div class="small mb-4"><%= comment.content%></div>
　<!--<p>投稿日時：<%= @post.created_at.strftime('%Y/%m/%d') %></p>-->
<div class="small mb-4"><%= link_to salon_post_comment_path(@post,@comment), method: :delete, remote:true, data: {confirm: "本当に削除しますか？"} do %>
<i class="fas fa-trash" style="color: black;">
<% end %>
<% end %>
</div>

  </tbody>
</table>